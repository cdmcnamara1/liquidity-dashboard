{
  "version": 2,
  "functions": {
    "api-server/*.js": {
      "runtime": "nodejs18.x",
      "maxDuration": 10
    }
  },
  "builds": [
    { "src": "api-server/*.js", "use": "@vercel/node" },
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api-server/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
